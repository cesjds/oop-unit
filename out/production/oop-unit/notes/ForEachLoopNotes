/*
    enhanced or for/each loops to use in your new
    state.State database class NOT your StateTester class:


    for(int i = 0; i < list.size(); i++) {
      state.State s = list.get(i)
      s.toString();
    }

    for(state.State s: states)
        s.compareTo(s);
 */